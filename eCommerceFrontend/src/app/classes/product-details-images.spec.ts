import { ProductDetailsImages } from './product-details-images';

describe('ProductDetailsImages', () => {
  it('should create an instance', () => {
    expect(new ProductDetailsImages()).toBeTruthy();
  });
});
