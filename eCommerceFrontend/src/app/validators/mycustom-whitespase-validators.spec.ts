import { MycustomWhitespaseValidators } from './mycustom-whitespase-validators';

describe('MycustomWhitespaseValidators', () => {
  it('should create an instance', () => {
    expect(new MycustomWhitespaseValidators()).toBeTruthy();
  });
});
